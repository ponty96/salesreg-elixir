# Erlang version
erlang_version=21.0

# Elixir version
elixir_version=1.7.4

# Always rebuild from scratch on every deploy?
always_rebuild=true

# A command to run right after compiling the app
hook_post_compile="POOL_SIZE=1 mix ecto.migrate"