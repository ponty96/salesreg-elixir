{
  "private": true,
  "scripts": {
    "release": "RELEASE=$(git rev-parse HEAD)-$(date +%s)-client",
    "deploy": "node_modules/.bin/webpack --config webpack.prod.js -p",
    "eslint": "node_modules/.bin/eslint js/",
    "tslint": "tslint -c tslint.json 'js/**/*.{ts,tsx}'",
    "pretty": "prettier --write --tab-width 2 --no-semi --single-quote 'js/**/*.ts'",
    "stylelint": "node_modules/.bin/stylelint 'css/**/*.s+(a|c)ss'",
    "watch": "node_modules/.bin/webpack --config webpack.dev.js --watch-stdin --progress --color",
    "analyzer": "node_modules/.bin/webpack --config webpack.dev.js --env.runAnalyzer=true --watch-stdin --progress --color"
  },
  "dependencies": {
    "moment": "^2.24.0"
  }
}
